# SQLite local (Prisma). Ruta relativa a prisma/.
DATABASE_URL="file:./dev.db"

# DevExpert Inference (IA). Opcional: sin clave, las funciones de IA degradan
# con un mensaje claro. No commitear la clave real.
DEVEXPERT_API_KEY=
